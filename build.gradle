defaultTasks 'build'

wrapper {
    description "Regenerates the Gradle Wrapper files"
    gradleVersion = '6.0.1'
    distributionUrl = "http://services.gradle.org/distributions/gradle-6.0.1-all.zip"
}
